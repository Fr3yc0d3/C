/*
 * Ok now we are in output section yeah 
 * so lets have some output actually we have already done this 
*/

#include <stdio.h>
int main(){
    char a = getchar();
    printf("your result = ");
    putchar(a);
    return 0;
}
